use std::error::Error;

pub(crate) fn add(_args: &clap::ArgMatches) -> Result<(), Box<dyn Error>> {
  Ok(())
}

pub(crate) fn edit(_args: &clap::ArgMatches) -> Result<(), Box<dyn Error>> {
  Ok(())
}
